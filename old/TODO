Main TODOs
==========

* install packages:
  core / minimal
  grml-small
  full grml
  own-selection
  kernel + modules
* initrd creation
* squashfs generation
* iso generation

Checkout
========

* initrd creation via genext2fs
